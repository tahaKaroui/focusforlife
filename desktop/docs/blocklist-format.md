# Blocklist Format (v1)

File: one domain per line.
- Lines starting with `#` are comments.
- Empty lines are ignored.
- Domains are normalized to lowercase; trailing dots are removed.

Example:
# social
example.com
news.example.net
